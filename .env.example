# Copy this file to ".env" in the project root and paste your key ONLY in .env (never here).
# The real ".env" file is gitignored — never commit secrets into .env.example.
#
#   cp .env.example .env
#
GEMINI_API_KEY=

# Optional — overrides ai_assistant.yml model when set:
# GEMINI_MODEL=gemini-2.5-flash
